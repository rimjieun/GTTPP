import React from 'react';
import { ColLayout } from './../../common';

const styles = {
    text: {
        fontSize: '20px',
        lineHeight: '50px'
    }
};

const CampusResources = props => (
    <ColLayout title='CAMPUS RESOURCES'>
        <p className="header center">Links to Campus Resources</p>
    </ColLayout>
);

export default CampusResources;