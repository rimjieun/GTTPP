import React from 'react';
import { ColLayout } from './../../common';

const styles = {
    text: {
        fontSize: '20px',
        lineHeight: '50px'
    }
};

const AcademicServices = props => (
    <ColLayout title='ACADEMIC SERVICES'>
        <p className="header center">Link to academics & pictures</p>
    </ColLayout>
);

export default AcademicServices;