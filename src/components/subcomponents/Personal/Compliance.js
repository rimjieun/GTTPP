import React from 'react';
import { ColLayout } from './../../common';

const styles = {
    text: {
        fontSize: '20px',
        lineHeight: '50px'
    }
};

const Compliance = props => (
    <ColLayout title='COMPLIANCE'>
        <p className="header center">Link to compliance & pictures</p>
    </ColLayout>
);

export default Compliance;