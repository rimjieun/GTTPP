import React from 'react';
import { ColLayout } from './../../common';

const styles = {
    text: {
        fontSize: '20px',
        lineHeight: '50px'
    }
};

const SportsMedicine = props => (
    <ColLayout title='SPORTS MEDICINE'>
        <p className="header center">Link to Sports Medicine & pictures</p>
    </ColLayout>
);

export default SportsMedicine;