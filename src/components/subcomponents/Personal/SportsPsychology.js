import React from 'react';
import { ColLayout } from './../../common';

const styles = {
    text: {
        fontSize: '20px',
        lineHeight: '50px'
    }
};

const SportsPsychology = props => (
    <ColLayout title='SPORTS PSYCHOLOGY'>
        <p className="header center">Dr. Kensa Gunter information and picture </p>
    </ColLayout>
);

export default SportsPsychology;