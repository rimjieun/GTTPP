import React from 'react';
import { ColLayout } from './../../common';

const styles = {
    text: {
        fontSize: '20px',
        lineHeight: '50px'
    }
};

const ExecutiveBoard = props => (
    <ColLayout title='EXECUTIVE BOARD'>
        <p className="header center">Pictures</p>
    </ColLayout>
);

export default ExecutiveBoard;