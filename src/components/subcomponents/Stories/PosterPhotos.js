import React from 'react';
import { ColLayout } from './../../common';

const styles = {
    text: {
        fontSize: '20px',
        lineHeight: '50px'
    }
};

const PosterPhotos = props => (
    <ColLayout title=''>
        <p className="header center">Poster Photos</p>
    </ColLayout>
);

export default PosterPhotos;