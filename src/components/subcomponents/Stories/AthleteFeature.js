import React from 'react';
import { ColLayout } from './../../common';

const styles = {
    text: {
        fontSize: '20px',
        lineHeight: '50px'
    }
};

const AthleteFeature = props => (
    <ColLayout title='STUDENT-ATHLETE FEATURE'>
        <p className="header center">Student-Athlete Feature</p>
    </ColLayout>
);

export default AthleteFeature;